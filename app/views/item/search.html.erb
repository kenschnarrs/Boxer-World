<h1>Search results for "<%= @query %>"</h1>

<% if @results.any? %>
  <ul>
    <% @results.each do |result| %>
      <li><%= result.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>No results found.</p>
<% end %>
