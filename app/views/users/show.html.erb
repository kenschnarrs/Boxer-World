  <!-- Calvin Gott, Dropdown menu, takes a user to show page, and then allows them to update username and blurd-->
<head>
    <title>User Customization</title>
  
  </head>
<h1> User Customization </h1>
<strong>Welcome <%=@user.username%></strong> 

<p>

  You can Change your Username and Bio Below
</p>
<%= bootstrap_form_with model: @user, url: users_path(@user), method: :patch, data: { turbo: false } do |f| %>
  <%= f.text_field :username %>
  <%= f.text_area :blurb %>
  <%= f.submit %>
<% end %>







